class Solution:
    def shuffle(self, nums: List[int], n: int) -> List[int]:
        #res=[0]*len(nums)
        res = []
        j=0
        
        for i in range(n):
                res.append(nums[i])
                res.append(nums[n+i])
        return res
